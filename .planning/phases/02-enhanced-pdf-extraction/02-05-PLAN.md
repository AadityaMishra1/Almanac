---
phase: 02-enhanced-pdf-extraction
plan: 05
type: execute
wave: 3
depends_on: ["02-03", "02-04"]
files_modified:
  - components/syllabus-to-calendar.tsx
  - app/api/parse/route.ts
  - lib/events/extract.ts
autonomous: false

must_haves:
  truths:
    - "User uploads a text-based PDF and sees events with confidence scores in preview table"
    - "User can edit event title, date, and type before syncing"
    - "Low-confidence events are visually highlighted and pre-deselected"
    - "Events sync to database and Google Calendar as before"
    - "Extraction metadata (text vs OCR, page count) is visible"
  artifacts:
    - path: "components/syllabus-to-calendar.tsx"
      provides: "Fully integrated upload-to-preview-to-sync flow"
    - path: "app/api/parse/route.ts"
      provides: "Enhanced parse endpoint returning confidence-scored events"
  key_links:
    - from: "components/syllabus-to-calendar.tsx"
      to: "app/api/parse/route.ts"
      via: "fetch POST /api/parse"
      pattern: 'fetch.*"/api/parse"'
    - from: "app/api/parse/route.ts"
      to: "lib/pdf/index.ts"
      via: "extractPdfContent call"
      pattern: "extractPdfContent"
    - from: "app/api/parse/route.ts"
      to: "lib/events/extract.ts"
      via: "extractEventsWithConfidence call"
      pattern: "extractEventsWithConfidence"
---

<objective>
End-to-end integration testing and bug fixes for the enhanced PDF extraction pipeline. Verify that the full flow works: upload PDF -> detect type -> extract text (or OCR) -> extract events with confidence -> show preview with badges -> allow editing -> sync to calendar. Fix any integration issues between Plans 01-04.

Purpose: Ensure all Phase 2 components work together as a cohesive system. This is the final integration step before marking Phase 2 complete.
Output: Working end-to-end flow with any bug fixes applied
</objective>

<execution_context>
@/Users/aadityamishra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aadityamishra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-enhanced-pdf-extraction/02-01-SUMMARY.md
@.planning/phases/02-enhanced-pdf-extraction/02-02-SUMMARY.md
@.planning/phases/02-enhanced-pdf-extraction/02-03-SUMMARY.md
@.planning/phases/02-enhanced-pdf-extraction/02-04-SUMMARY.md

# Key integration files
@app/api/parse/route.ts
@components/syllabus-to-calendar.tsx
@components/events-preview-table.tsx
@lib/pdf/index.ts
@lib/events/extract.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration testing and bug fixes</name>
  <files>
    components/syllabus-to-calendar.tsx
    app/api/parse/route.ts
    lib/events/extract.ts
  </files>
  <action>
    Run `npm run build` and `npx tsc --noEmit` to catch any type errors across the integration boundary.

    Then run `npm run dev` and test the following scenarios mentally by tracing the code flow. Fix any issues found:

    **Integration checklist:**

    1. **Type compatibility:** Verify that the parse route response shape matches what SyllabusToCalendar expects:
       - Response must include `events: EventWithConfidence[]` (not old `SyllabusEvent[]`)
       - Response must include `metadata` object with method, pageCount, totalEvents, highConfidence, needsReview
       - Response must include `courseId`, `eventIds` for database-backed sync flow

    2. **Confidence merge flow:** After parse, SyllabusToCalendar loads events from database (via getEvents) and merges confidence data from parse response. Verify:
       - Database events have IDs but no confidence
       - Parse response events have confidence but may not have IDs
       - Merge must match by title+date to attach confidence to database events
       - If no match found, default to medium confidence (0.5)

    3. **Preview table data flow:** Verify EventsPreviewTable receives PreviewRow[] with:
       - All EventWithConfidence fields (title, date, type, description, confidence)
       - `selected: boolean` (pre-selected based on confidence >= 0.6)
       - `id: string` (database event ID for sync)

    4. **Sync flow unchanged:** After user edits and clicks sync:
       - Selected event IDs passed to syncEventsToCalendar (existing server action)
       - User edits in preview table should be saved back to database before sync (if user changes a date/title, that change must persist)
       - Add a pre-sync save step if not already present: for each modified row, call updateEvent server action

    5. **Error handling:** Verify error states work:
       - Empty PDF (no text, no OCR text)
       - Groq API failure (should fall back gracefully)
       - No events extracted (show friendly message)

    Fix any TypeScript errors, import issues, or data flow mismatches found during this integration review. The goal is a clean build and correct data flow, not functional testing (that's the checkpoint).

    **Specific fix: Pre-sync update**
    If user edits events in preview table, those edits are only in React state. Before syncing to Google Calendar, we need to persist edits back to database. Add logic to SyllabusToCalendar's handleSync:
    - Before calling syncEventsToCalendar, check if any rows have been modified (compare against original parse data)
    - For modified rows with database IDs, call the existing updateEvent server action to save changes
    - Then proceed with sync as before

    To track modifications, add an `originalData` ref that stores the initial parse result, and compare current rows against it before sync.
  </action>
  <verify>
    1. `npm run build` succeeds with zero errors
    2. `npx tsc --noEmit` passes
    3. No import errors across the integration boundary
    4. Response types are consistent between route and UI
  </verify>
  <done>
    - TypeScript compiles cleanly across all Phase 2 files
    - Parse route response matches UI expectations (events with confidence, metadata)
    - Confidence merge logic correctly attaches scores to database events
    - Pre-sync update saves user edits before Google Calendar sync
    - Error handling covers empty/failed extractions
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 2 Enhanced PDF Extraction pipeline:
    - OCR detection and extraction for scanned PDFs (lib/pdf/)
    - Confidence scoring for all extracted events (lib/events/)
    - Table extraction for spreadsheet-style PDFs
    - Preview UI with confidence badges, inline editing, type dropdowns
    - Extraction metadata display (OCR vs text, page count, review-needed count)
    - Pre-sync save for user edits
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000
    2. Enter a course name (e.g., "Data Structures")
    3. Upload a text-based PDF syllabus
    4. Verify:
       - Extraction metadata banner appears (text extraction, page count, confidence stats)
       - Events appear in preview table with confidence badges (green/amber/red pills)
       - Each event has: editable title, date picker, type dropdown, confidence badge
       - Low-confidence events (red) are pre-deselected
       - You can edit event title, change date, change type via dropdown
    5. Select events and click "Sync to Google Calendar"
    6. Verify sync completes and events appear in Google Calendar
    7. (Optional) If you have a scanned PDF, upload it and verify OCR is triggered (metadata shows "OCR")
  </how-to-verify>
  <resume-signal>Type "approved" if flow works, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. Full upload-to-preview-to-sync flow works end-to-end
3. Confidence badges visible and color-coded correctly
4. Type dropdown works with 6 options
5. Date picker allows date changes
6. Extraction metadata visible
7. Sync to Google Calendar still works
</verification>

<success_criteria>
- User can upload PDF and see events with confidence scores
- User can edit events inline (title, date, type, description)
- Low-confidence events visually highlighted and pre-deselected
- Sync to Google Calendar works after edits
- No TypeScript errors, clean build
</success_criteria>

<output>
After completion, create `.planning/phases/02-enhanced-pdf-extraction/02-05-SUMMARY.md`
</output>
