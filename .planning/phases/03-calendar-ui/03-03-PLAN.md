---
phase: 03-calendar-ui
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - components/calendar/calendar-toolbar.tsx
  - components/calendar/calendar-view.tsx
  - components/calendar/event-detail-modal.tsx
  - app/calendar/page.tsx
  - app/globals.css
autonomous: false

must_haves:
  truths:
    - "Calendar renders usably on mobile screens (< 768px) with touch-friendly controls"
    - "Calendar auto-defaults to day view on mobile screens"
    - "Toolbar buttons are large enough to tap reliably on mobile (min 44px)"
    - "Event detail modal is full-screen on mobile for readability"
    - "View switching uses a compact dropdown on mobile instead of three buttons"
    - "Calendar page and upload page are connected via navigation"
  artifacts:
    - path: "components/calendar/calendar-toolbar.tsx"
      provides: "Responsive toolbar with mobile dropdown for view switching"
      min_lines: 60
    - path: "components/calendar/calendar-view.tsx"
      provides: "Responsive calendar with mobile view auto-switch"
      min_lines: 100
    - path: "app/globals.css"
      provides: "RBC CSS overrides for mobile and Tailwind conflict fixes"
      contains: "rbc"
  key_links:
    - from: "components/calendar/calendar-view.tsx"
      to: "components/calendar/calendar-toolbar.tsx"
      via: "responsive toolbar component"
      pattern: "CalendarToolbar"
    - from: "app/calendar/page.tsx"
      to: "app/page.tsx"
      via: "navigation links between pages"
      pattern: "Link.*href"
---

<objective>
Make the calendar fully responsive on mobile devices with touch-friendly controls, auto-switch to day view on small screens, and verify the complete calendar UI end-to-end.

Purpose: 60% of expected traffic is mobile. Without responsive design, the calendar is unusable for most students. This plan covers CAL-07 (mobile responsiveness) and provides the final verification checkpoint for Phase 3.

Output: Mobile-responsive calendar with touch-friendly toolbar, compact view switching, and full-screen modal on mobile. Visual verification of all Phase 3 features.
</objective>

<execution_context>
@/Users/aadityamishra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aadityamishra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-calendar-ui/03-RESEARCH.md
@.planning/phases/03-calendar-ui/03-01-SUMMARY.md
@.planning/phases/03-calendar-ui/03-02-SUMMARY.md

# Files being modified
@components/calendar/calendar-toolbar.tsx
@components/calendar/calendar-view.tsx
@components/calendar/event-detail-modal.tsx
@app/calendar/page.tsx
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make calendar responsive for mobile devices</name>
  <files>
    components/calendar/calendar-toolbar.tsx
    components/calendar/calendar-view.tsx
    components/calendar/event-detail-modal.tsx
    app/calendar/page.tsx
    app/globals.css
  </files>
  <action>
    1. Create a `useIsMobile` hook (inline in calendar-view.tsx or as a small utility):
       - Use useState + useEffect with window.matchMedia('(max-width: 767px)')
       - Listen for changes and update state
       - Return boolean `isMobile`
       - Default to false on server (SSR safe)

    2. Update `components/calendar/calendar-toolbar.tsx` for responsive layout:
       - Accept `isMobile` prop (boolean)
       - Desktop (isMobile = false): Keep existing layout — Today button, Prev/Next, label, Month/Week/Day buttons
       - Mobile (isMobile = true):
         a) First row: Prev chevron, date label (centered, truncated), Next chevron — all in a compact horizontal strip
         b) Second row: Today button (small) on left, view selector on right
         c) Replace three view buttons with a single Radix Select dropdown (`<Select>`) showing current view name
            - Import Select, SelectTrigger, SelectContent, SelectItem from @radix-ui/react-select
            - Use inline Radix Select (no need for the ui/select.tsx wrapper — keep it simple with direct Radix imports)
            - Trigger shows current view name with a ChevronDown icon
            - Items: Month, Week, Day
         d) All buttons: min-h-[44px] min-w-[44px] for touch targets
         e) Date label: text-base (not text-lg) to fit mobile widths
       - Wrap layout in flex-col on mobile, flex-row on desktop using Tailwind responsive classes

    3. Update `components/calendar/calendar-view.tsx` for mobile:
       - Add useIsMobile hook
       - Pass isMobile to CalendarToolbar
       - Auto-set default view: if isMobile is true and view is 'month' on initial render, switch to 'day'
         (Use useEffect that runs when isMobile changes: if isMobile && view === 'month', setView('day'))
       - Adjust calendar container height: `h-[calc(100vh-120px)]` on desktop, `h-[calc(100vh-80px)]` on mobile
       - Pass `dayLayoutAlgorithm="no-overlap"` to Calendar for cleaner mobile rendering in week view

    4. Update `components/calendar/event-detail-modal.tsx` for mobile:
       - Add responsive classes to DialogContent:
         - Mobile: `sm:max-w-lg max-w-[95vw] max-h-[90vh] overflow-y-auto` so modal nearly fills screen
         - The dialog overlay should be touchable to dismiss
       - Edit form inputs: ensure full width on mobile (w-full)
       - Buttons in edit mode: stack vertically on mobile (`flex-col sm:flex-row`)

    5. Update `app/calendar/page.tsx` header for mobile:
       - Make header responsive: stack title and nav link vertically on mobile
       - Reduce padding: p-4 on mobile, p-6 on desktop (`p-4 sm:p-6`)
       - Ensure the "back to upload" link is easily tappable

    6. Add RBC CSS overrides in `app/globals.css`:
       - Add targeted overrides to fix Tailwind base reset conflicts with RBC:
         ```css
         /* react-big-calendar Tailwind overrides */
         .rbc-calendar {
           font-family: inherit;
         }
         .rbc-header {
           padding: 8px 4px;
           font-weight: 600;
           font-size: 0.75rem;
         }
         .rbc-event {
           padding: 2px 4px;
         }
         .rbc-show-more {
           color: #3b82f6;
           font-size: 0.75rem;
           font-weight: 500;
         }
         /* Mobile: make day column headers shorter */
         @media (max-width: 767px) {
           .rbc-header {
             font-size: 0.65rem;
             padding: 4px 2px;
           }
           .rbc-time-slot {
             min-height: 30px;
           }
           .rbc-event {
             font-size: 0.65rem;
             padding: 1px 3px;
           }
         }
         ```
       - Do NOT import RBC CSS in globals.css — it must stay imported in calendar-view.tsx (component-level import). The globals.css file only adds overrides/fixes.
  </action>
  <verify>
    Run `npx tsc --noEmit` to confirm no type errors.
    Test at desktop width (>= 1024px):
    - Toolbar shows all three view buttons inline
    - Calendar renders at full width with comfortable spacing
    Test at mobile width (< 768px) using browser DevTools device toolbar:
    - Calendar auto-switches to day view
    - Toolbar shows compact layout with select dropdown for views
    - Buttons are large enough to tap (44px minimum)
    - Event detail modal nearly fills screen
    - No horizontal scrolling or overflow
  </verify>
  <done>
    Calendar toolbar is responsive: full buttons on desktop, compact dropdown on mobile.
    Calendar auto-defaults to day view on mobile screens.
    All touch targets are at least 44px.
    Event detail modal is near-full-screen on mobile.
    RBC CSS overrides prevent Tailwind conflicts.
    No horizontal overflow on any screen size.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 3 Calendar UI with all 11 requirements:
    - CAL-01: Month/week/day view switching
    - CAL-02: Click event to view details in modal
    - CAL-03: Edit event details inline from calendar
    - CAL-04: Events color-coded by course
    - CAL-05: Conflicting events show warning indicator
    - CAL-06: NCSU academic calendar overlay (Spring Break, Finals, etc.)
    - CAL-07: Mobile-responsive with touch-friendly controls
    - CAL-08: Date navigation with prev/next and date picker
    - CAL-09: Current date/time indicator in week/day views
    - CAL-10: All-day events above timeline in day/week views
    - CAL-11: Today button returns to current date
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Navigate to http://localhost:3000/calendar

    **Desktop verification (browser at full width):**
    3. Confirm month view renders with events color-coded by course
    4. Click "Week" button — verify week view with timeline and all-day row
    5. Click "Day" button — verify day view with hourly slots
    6. Click "Today" — confirm it returns to current date
    7. Click Prev/Next arrows — confirm date navigation
    8. Look for NCSU academic calendar dates (Spring Break Mar 16-21, Finals Apr 30-May 7) as faded background blocks
    9. Click any event — confirm detail modal opens with title, date, time, type, course
    10. Click "Edit" in modal — confirm form appears with pre-filled values
    11. Change event title, click "Save" — confirm calendar updates
    12. Verify current time indicator (red line) in week/day views

    **Mobile verification (browser DevTools, iPhone 14 or similar):**
    13. Toggle device toolbar to ~390px width
    14. Confirm calendar shows day view by default
    15. Confirm toolbar is compact with dropdown for view switching
    16. Tap an event — confirm modal opens near-full-screen
    17. Confirm all buttons are easy to tap (no tiny targets)
    18. Confirm no horizontal scrolling

    **Navigation verification:**
    19. From /calendar, click link to go back to upload page
    20. From /, click link to go to calendar page
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 3, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. All 11 CAL requirements verified visually
3. Mobile layout works on 390px width without overflow
4. Desktop layout uses full available width
5. Event editing persists to database
6. Navigation between / and /calendar works both directions
7. No console errors in browser
</verification>

<success_criteria>
- Calendar is fully usable on mobile (< 768px) with day view default
- Toolbar adapts: full buttons on desktop, compact dropdown on mobile
- Touch targets >= 44px on all interactive elements
- Event modal is near-full-screen on mobile
- RBC CSS renders correctly (no Tailwind conflicts)
- All 11 CAL requirements from roadmap are satisfied
- Human verification checkpoint passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-calendar-ui/03-03-SUMMARY.md`
</output>
